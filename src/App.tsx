import React from 'react';
import { Button } from 'antd';
import styles  from "./app.scss";

function App() {
  return (
    <div className={ styles.color }>
      ddd
      <Button>sss</Button>
    </div>
  );
}

export default App;
